        for i in range(numCourses):

            
            for nei in preq[i]:
                    if not dfs(nei):
                        return False
            return True
            visited.add(i)
                return False
            if i in visited:
        def dfs(i):
        completed = set()
            preq[a].append(b)
            completed.add(i)
            visited = set()
            if not dfs(i):
                if nei not in completed:
