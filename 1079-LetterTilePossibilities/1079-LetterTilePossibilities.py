                    dfs(idx+1,st+tile,dic)
                    dic[tile] += 1
            return

            
            


        dfs(-1,"",count)
        return len(answer)

                    dic[tile] -= 1
                if dic[tile] > 0:
            for tile in tiles:
            dfs(idx+1,st,dic)
                return
            if idx == L:
